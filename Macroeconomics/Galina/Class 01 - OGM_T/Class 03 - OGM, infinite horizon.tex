\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\DeclareUnicodeCharacter{21D2}{\ifmmode\Rightarrow\else{$\Rightarrow$}\fi}

\begin{document}
Consider the same environment as before, with one key difference: $T=\infty$

⇒ The Socral Planner / Robinson crusol / agent's (dependeng on interpretation) problem is:

$$
\begin{array}{ll}
\max _{\left.i C_{t}, k_{t+1}\right\}_{t=0}^{\infty}} \sum_{t=0}^{\infty} \beta^{+} u(G) \\
\text { s.t. } & C_{t}+k_{t+1} \leq f\left(k_{t}\right), t \geq 0 \quad(t=1,2, \ldots) \\
& k_{t+1} \geq 0, t \geq 0 \\
& k_{0} \text { is given } \\
& \left.C_{a} \geq 0 \text { (unnecessary if } u^{\prime}(0) \rightarrow \infty\right)
\end{array}
$$

Remark: $\beta \in(0,1)$ is cencial!\\
If $\beta \geqslant 1 \Rightarrow$ the objective function is infinite ⇒ the moblem is not well-defined\\
Trivially, $c_{+}+k_{+\ldots} \in f / k_{+}$) (not optimal to waste resources)

⇒ Rewrite (*) as\\
\includegraphics[max width=\textwidth, alt={}, center]{575108fe-f0a6-4738-8032-3b1ca41a5ec1-1_463_1940_2075_73}

If $f(0)=0$ and $u(0)=\infty \Rightarrow$ the constraints are not binding ⇒ the problem is unconstainal, Just take (FOC) w.r.t. (kt+1):

$$
-\beta^{+} u^{\prime}\left(f\left(k_{+}\right)-k_{t+1}\right)+\beta^{++1} \cdot f^{\prime}\left(k_{t+1}\right) \cdot u^{\prime}\left(f\left(k_{t+1}\right)-k_{t+2}\right)=0
$$

⇒ get a familiar second-order difference equation:\\
(EE) $u^{\prime}\left(f\left(k_{+}\right)-k_{++1}\right)=\beta f^{\prime}\left(k_{++1}\right) \cdot u^{\prime}\left(f\left(k_{++1}\right)-k_{++2}\right), t \geqslant 1$\\
$k_{0}$ is given

Notice, however, that it has only one terminal condition (ko is given) ⇒\\
⇒ (EE) has an infinite \# solutrons (continuem):\\
$\forall k_{1}: \exists k_{2} \Rightarrow \exists k_{3} \ldots$\\
undetermined\\
(becouse $\nexists \quad k_{T+1}=0$ )

Under centain conditions on $U(\cdot)$ and $f(\cdot)$, the sequence of $\left\{k_{++}\right\}_{t=0}^{\infty}$ solves (SP) if and only if • $\left\{k_{++1}\right\}_{t}^{\infty}=0$ solves (EE)

\begin{itemize}
  \item satisfies TRONSVERSALITY condition:\\
(TC)
\end{itemize}

$$
\lim _{t \rightarrow \infty} \beta^{+} u^{\prime}\left(f\left(k_{+}\right)-k_{++1}\right) \circ f^{\prime}\left(k_{+}\right) \cdot k_{+}=0
$$

PII n'n. ontro unit of 1

\section*{PV of one extra unit of $k_{4}$}
\section*{pv of extra utility generated by $k_{+}$ \\
 in periodt.}
For brevity, denote $\underbrace{F\left(X_{+}, X_{++1}\right)}=u^{\square}\left(f\left(X_{+}\right)-X_{+1}\right)$ the proof holds for more general case.

\section*{Theorem:}
Suppose $F(\because)$ is contrunously differentiable, concave, $T$ in the first argument, $\downarrow$ in the second argument and bounded.\\
Then $\left\{x_{+}\right\}_{+=0}^{\infty}$ solves (SP) if and only if $\{x+\}_{t=0}^{\infty}$ satisfies (EE) \& (TC):

$$
\begin{aligned}
& (E E): \quad F_{2}\left(X_{t}, X_{t+1}\right)+\beta F_{1}\left(X_{t+1}, X_{t+2}\right)=0 \\
& (T C): \quad \lim _{+\rightarrow \infty} \beta^{+} F_{1}\left(X_{t}, X_{t+1}\right) \cdot X_{t}=0
\end{aligned}
$$

Proof: (1) Sufficiency\\
(2) necessity

\section*{1. Sufticency}
Let $\left\{x_{0}^{*}, x_{1}^{*}, \ldots, x_{t}^{*}, \ldots\right\}$ satisty (EE) and (TC) Need to show that $\forall\left\{x_{-1}\right\}_{+=0}^{\infty} \neq\left\{x_{+}^{*}\right\}_{+=0}^{\infty}$,

$$
\sum_{t=0}^{\infty} \beta^{+} F\left(x_{t}^{*}, x_{t+1}^{*}\right) \geqslant \sum_{t=0}^{\infty} \beta^{+} F\left(x_{t}, x_{t+1}\right)
$$

$D=\lim _{T \rightarrow \infty} \sum_{t=0}^{T} \beta^{t}\left[F\left(x_{t}^{*}, x_{t+1}^{*}\right)-F\left(x_{t}, x_{t+1}\right)\right] \geqq 0$\\
need to show

$$
\begin{aligned}
& F(x, y) \text { - concave ⇒ } \\
& \Rightarrow F\left(x^{*}, y^{*}\right)-F(x, y) \geqslant F_{x}\left(x^{*}, y^{*}\right)\left(x^{*}-x\right)+F_{y}\left(x^{*}, y^{*}\right)\left(y^{*}-y\right) \\
& \Rightarrow D \geqslant \lim _{T \rightarrow \infty} \sum_{t=0}^{T} \beta^{t} \cdot\left[F_{1}\left(x_{t}^{*}, x_{t+1}^{*}\right) \cdot\left(x_{t}^{*}-x_{t}\right)+F_{2}\left(x_{t}^{*}, x_{t+1}^{*}\right)\left(x_{t+1}^{*}-x_{t+1}\right)\right] \\
& =\lim _{T \rightarrow \infty} F_{1}\left(x_{0}^{*}, x_{1}^{*}\right) \underbrace{\left(x_{0}^{*}-x_{0}\right)}_{\substack{\text { since } x_{0} \text { is } \\
\text { sven }}}+\underset{=0 \text { by }( \pm E)}{F_{2}\left(x_{0}^{*}, x_{1}^{*}\right)}\left(x_{1}^{*}-x_{1}\right)+ \\
& +\beta E_{1}\left(x_{1}^{*}, x_{2}^{*}\right)\left(x_{1}^{*}-x_{1}\right)+\beta F_{2}\left(x_{1}^{*}, x_{2}^{*}\right)\left(x_{2}^{*}-x_{2}\right)+\cdots 1 \\
& +\beta^{\top} F_{1}\left(x_{i}^{*}, x_{T+1}^{*}\right)\left(x_{T}^{*}-x_{T}\right)+\beta^{\top} F_{2}\left(x_{T}^{*}, x_{T+1}^{*}\right)\left(x_{T+1}^{*}-x_{T+1}\right) \\
& =\underbrace{\lim _{T \rightarrow \infty} \beta^{\top} F_{2}\left(x_{T}^{*}, x_{T+1}^{*}\right) x_{T+1}^{*}}_{\geqslant 0}-\underbrace{\lim _{T \rightarrow \infty} \beta^{\top} \underbrace{F_{2}\left(x_{T}^{*}, x_{T+1}^{*}\right)}_{<0}}_{|\log (E E)|=}-x_{T+1} \geq 0 \\
& =-\lim _{T \rightarrow \infty} \beta^{T+1} F_{1}\left(X_{T+1}^{*}, X_{T+2}^{*}\right) \cdot X_{T+1}^{*}=0 \text { by (TC) }
\end{aligned}
$$

Thus if $\left\{x_{+}^{*}\right\}_{+=0}^{\infty}$ satisfies (EE) $+(T C) \Rightarrow\left\{x_{-}^{*}\right\}_{+=0}^{\infty}$ solves (SP)\\
2. Necessity

Suppose that $\left\{X_{+}^{*}\right\}_{+=0}^{\infty}$ solves $(S P) \Rightarrow$\\
⇒ any other $\{x+\}_{1}^{\infty}=0$ delivers less life-time utility.\\
In particular, consider $\left\{x_{*}\right\}=\left\{x_{0}^{*}, x_{1}^{*}, \ldots, x_{T}^{*}, \lambda x_{T+1}^{*}, \lambda x_{T+2}^{*}, \ldots\right\}$\\
This is feasible $(x+\geq 0) \lambda \in[0,1)$

$$
\begin{aligned}
\Rightarrow & \sum_{t=0}^{\infty} \beta^{*}\left[F\left(x_{t}^{*}, x_{t+1}^{*}\right)-F\left(x_{t}, x_{t+1}\right)\right] \geqslant 0 \\
& \beta^{\top}\left[F\left(x_{T}^{*}, x_{T+1}^{*}\right)-F\left(x_{T}^{*}, \lambda x_{t+1}^{*}\right)\right]+\sum_{t=T+1}^{\infty} \beta^{*}\left[F\left(x_{t}^{*}, x_{t+1}^{*}\right)-F\left(\lambda x_{t}^{*}, \lambda x_{t+1}^{*}\right)\right] \geqslant 0 \\
= & \beta^{\top} \frac{F\left(x_{T}^{*}, x_{T+1}^{*}\right)-F\left(x_{t}^{*}, \lambda x_{t+1}^{*}\right)}{1-\lambda} \leqslant \sum_{t=T+1}^{\infty} \beta^{+} \frac{F\left(x_{t}^{*}, x_{t+1}^{*}\right)-F\left(\lambda x_{t}^{*}, \lambda x_{t+1}^{*}\right)}{1-\lambda}
\end{aligned}
$$

$$
\begin{aligned}
& \text { take lim } \\
& F \text {-concave in each arg. ⇒ } \\
& \text { take } \lim _{\lambda \rightarrow 1}
\end{aligned}
$$


\end{document}